if a human model is selected, add that person to generation parameters
this is low priority

need to have a list of human models that are present in the latent space of each model stored in the DB

if a model is picked exists in the latent space of another stable-diffusion-checkpoint, need to send override to change that model